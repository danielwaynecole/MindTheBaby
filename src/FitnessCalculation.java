
public class FitnessCalculation {

    // Calculate inidividuals fittness by comparing it to our candidate solution
    static int getFitness(Room individual) {
        int fitness = 0;
        return fitness;
    }
}
